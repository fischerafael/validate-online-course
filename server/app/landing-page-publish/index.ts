import { LandingPageContent } from "@/client/entities";

export const useCaseLandingPagePublish = async ({
  content,
  version,
  slug,
}: {
  content: LandingPageContent;
  version: string;
  slug: string;
}) => {};
